{% extends '@Interfacing/base.html.twig' %}

{% block title %}Order Management{% endblock %}

{% block body %}
    {% for message in app.flashes('success') %}
        <div class="alert alert-success" role="status">{{ message }}</div>
    {% endfor %}
    {% for message in app.flashes('danger') %}
        <div class="alert alert-danger" role="alert">{{ message }}</div>
    {% endfor %}

    <div class="row g-4">
        <div class="col-12 col-xl-4">
            <div class="card shadow-sm">
                <div class="card-header">
                    <h1 class="h5 mb-0">Create demo order</h1>
                </div>
                <div class="card-body">
                    {{ form_start(createForm) }}
                    <div class="mb-3">{{ form_row(createForm.currency) }}</div>
                    <div class="mb-3">{{ form_row(createForm.grandTotal) }}</div>
                    {{ form_row(createForm.submit, { attr: { class: 'btn btn-primary' } }) }}
                    {{ form_end(createForm) }}
                </div>
            </div>
        </div>

        <div class="col-12 col-xl-8">
            <div class="card shadow-sm">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h2 class="h5 mb-0">Orders</h2>
                    <span class="badge text-bg-secondary">{{ orders|length }} records</span>
                </div>
                <div class="card-body">
                    {% if orders is empty %}
                        <p class="text-muted mb-0">Load fixtures or create an order to start the demo flow.</p>
                    {% else %}
                        <div class="vstack gap-3">
                            {% for order in orders %}
                                <div class="border rounded p-3 bg-white">
                                    <div class="d-flex flex-column flex-lg-row justify-content-between gap-3">
                                        <div>
                                            <div class="fw-semibold">{{ order.id }}</div>
                                            <div class="text-muted small">
                                                Status: {{ order.status|upper }}
                                                | Currency: {{ order.currency }}
                                                | Created: {{ order.createdAt|date('Y-m-d H:i') }}
                                            </div>
                                            <div class="small mt-2">
                                                Grand: {{ order.grandTotal }}
                                                | Paid: {{ order.paidTotal }}
                                                | Refunded: {{ order.refundedTotal }}
                                            </div>
                                        </div>
                                        <div class="row g-2 flex-grow-1">
                                            <div class="col-12 col-md-4">
                                                {{ form_start(paymentForms[order.slug]) }}
                                                {{ form_row(paymentForms[order.slug].amount) }}
                                                {{ form_row(paymentForms[order.slug].externalRef) }}
                                                {{ form_row(paymentForms[order.slug].submit, { attr: { class: 'btn btn-outline-success w-100' } }) }}
                                                {{ form_end(paymentForms[order.slug]) }}
                                            </div>
                                            <div class="col-12 col-md-4">
                                                {{ form_start(shipmentForms[order.slug]) }}
                                                {{ form_row(shipmentForms[order.slug].carrier) }}
                                                {{ form_row(shipmentForms[order.slug].note) }}
                                                {{ form_row(shipmentForms[order.slug].submit, { attr: { class: 'btn btn-outline-primary w-100' } }) }}
                                                {{ form_end(shipmentForms[order.slug]) }}
                                            </div>
                                            <div class="col-12 col-md-4">
                                                {{ form_start(refundForms[order.slug]) }}
                                                {{ form_row(refundForms[order.slug].amount) }}
                                                {{ form_row(refundForms[order.slug].reason) }}
                                                {{ form_row(refundForms[order.slug].submit, { attr: { class: 'btn btn-outline-danger w-100' } }) }}
                                                {{ form_end(refundForms[order.slug]) }}
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            {% endfor %}
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
