# v0.27 — Outbox Processor + RabbitMQ\n\n## Что добавлено\n- OutboxProcessor с публикацией в transport `order_outbox`, retry/fail.\n- Команда `order:outbox:process`.\n- Конфиг `messenger.yaml` (RabbitMQ + DLQ).\n- Поля outbox: status/attempts/last_error/routing_key.\n- Миграция `Version20251008_outbox_extend.php`.\n- Тест `OutboxProcessorTest`.\n\n## Интеграция\n1) Установи `RABBITMQ_DSN` (пример в `.env.example.append`).\n2) Импортируй `config/packages/messenger.yaml` и `config/services/order_outbox.yaml`.\n3) Прогони миграцию.\n4) Запусти: `php bin/console order:outbox:process --limit=100 --retry`.\n